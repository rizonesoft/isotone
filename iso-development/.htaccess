# Isotone Development Module Security
# Deny all direct access to automation internals
# Admin pages have their own .htaccess to allow access

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>