# Isotone - iso-content Directory Protection

# Allow access to uploaded files
<FilesMatch "\.(jpg|jpeg|png|gif|svg|pdf|doc|docx|zip|mp4|mp3)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Deny access to PHP files in uploads
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Directory listing disabled
Options -Indexes